"""Capa de dominio — entidades, value objects, reglas invariantes y puertos.

No depende de framework, BD ni proveedores externos (Clean Architecture).
Expone abstracciones (``Protocol``) que la infraestructura implementa
(Hexagonal Ports & Adapters, ADR-03).
"""
